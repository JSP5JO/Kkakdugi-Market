<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<!-- sql mapper   -->  
<mapper namespace="categorylist.xml"> <!-- namespace : 일종의 식별역할의 변수 -->
	
	<select id="getList" resultType="categorylist.xml" parameterType="java.util.Map">
		select * from categorylist f order by idx desc limit #{startNo},#{pageSize}
	</select>
	
	 <select id="getListAll" resultType="categorylist.xml">
		select * from categorylist  order by idx desc
	</select> 
	
	<select id="selectByIdx" resultType="categorylist.xml" parameterType="int">
		select * from categorylist where idx=#{idx} 
	</select>
	<select id="getCount" resultType="int" >
		select count(*) from categorylist
	</select>
	
	<insert id="insert" parameterType="categorylist.xml">
		insert into categorylist (userId,password,subject,content)
		values (#{userId},#{password},#{subject},#{content})
	</insert>
	
	<update id="update" parameterType="categorylist">
		update categorylist set subject =#{subject}, content=#{content}
		where idx=#{idx}
	</update>
	
	<delete id="delete" parameterType="java.util.Map"><!-- 글 비밀번호 확인하고 삭제 -->
		delete from categorylist where idx=#{idx} and password =#{password}
	</delete>
	
	<select id="passwordCheck" resultType="categorylist.xml" parameterType="java.util.Map">
		select * from categorylist where idx=#{idx} and password =#{password}
	</select>
	
	<!-- 조회수 증가 -->
	<update id="readCount" parameterType="int">
		update categorylist set readCount = readCount +1 where idx=#{idx}
	</update>

</mapper>